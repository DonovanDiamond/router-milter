# Router Milter

This is currently a very simple postfix milter that allows you to reject emails
sent to specific addresses or sent from specific addresses.

See main.go for the flags, and config.example.yaml for an example configuration
file.
